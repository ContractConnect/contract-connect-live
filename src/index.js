/* Reset all browser default spacing at top */
html, body {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Helvetica Neue', sans-serif;
  background-color: #f5f5f5;
}

/* Optional: Prevent horizontal overflow */
body {
  overflow-x: hidden;
}
